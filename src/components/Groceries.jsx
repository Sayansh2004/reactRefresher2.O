import React from 'react'

export default function Groceries() {
  return (
    <div>
        This is a huge component in itself and assume it has further large components. And that's where lazy loading comes into the picture.
    </div>
  )
}
